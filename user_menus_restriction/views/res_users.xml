<odoo>
    <data>
        <record id="hide_user_menus_reports" model="ir.ui.view">
            <field name="name">user.hide.menu.report</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">
                <xpath expr="//notebook" position="inside">
                    <page string="Hide Menus" invisible="is_admin">
                        <tree>
                            <field name="hide_ui_menu_ids"/>
                        </tree>
                    </page>
                </xpath>
                <field name="name" position="after">
                    <field name="is_admin" invisible="1"/>
                </field>
            </field>
        </record>

        <record id="ir_ui_menu_hide_users" model="ir.ui.view">
            <field name="name">restrict.menu.users</field>
            <field name="model">ir.ui.menu</field>
            <field name="inherit_id" ref="base.edit_menu_access"/>
            <field name="arch" type="xml">
                <xpath expr="//notebook" position="inside">
                    <page string="Restrict Users" name="restrict_users">
                        <tree>
                            <field name="hide_user_ids"/>
                        </tree>
                    </page>
                </xpath>
            </field>
        </record>

    </data>
</odoo>